const MuiDrawer = {
    defaultProps: {
        PaperProps: {
            sx: {
                backgroundColor: '#fff',
                boxShadow: 1
            }
        },
    }
};

export default MuiDrawer;
